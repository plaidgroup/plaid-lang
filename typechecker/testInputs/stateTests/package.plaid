package testInputs.stateTests;

state Top {
	val immutable Boolean b;
}

state Sub case of Top {
	val immutable Boolean b = new True;
}

val () -> unique Top createTop = fn () => new Top; // { val immutable Boolean b = new False; };

val () -> unique Sub createSub = fn () => new Sub { val immutable Boolean a = new True; };