package testInputs.functionTest;

state A { }

val unique A z = new A;

val (void, void)[immutable A z] -> void testFn = fn (void x, void y)[immutable A z] => unit;

val void u = unit;

val void t = testFn(u,u);