package testInputs.codeGenTest;

var foo = 5;
var bar = "hello";
val qux = unit;

method test(x) {
	var a = 3;
	val b = 4;
	val c = a + b;
	b = foo - 2;
	foo = b * c;
	val lam = fn (y) => java.lang.System.out.println(x);
	val lam2 = fn (y) => java.lang.System.out.println("Bang!");
	lam(a);
	lam(b);
	lam(c);
	lam(foo);
	lam(bar);
}

method main() {
	test(qux);
}
