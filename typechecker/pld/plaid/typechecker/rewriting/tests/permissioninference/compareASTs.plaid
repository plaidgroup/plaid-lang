package plaid.typechecker.rewriting.tests.permissioninference;

var compareASTs = fn(unique Tester tester, ast1, ast2) => 
{
	match (ast1 == ast2) {
		case True {
			tester.testPassed(true);
		}
		case False {
			tester.testPassed(false);
			printErrorASTs(oldAst,newAst,goalAst);
		}
	}
};