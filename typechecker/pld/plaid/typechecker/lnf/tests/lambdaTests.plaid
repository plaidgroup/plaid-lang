package plaid.typechecker.lnf.tests;

import plaid.ast.makeDereference;
import plaid.ast.makeLambda;
import plaid.ast.makeID;

val () -> void lambdaTests = fn() => {
	val e = makeDereference(makeToken(),makeID(makeToken(),"a"), makeID(makeToken(),"b"));
	val lam = makeLambda(makeToken(), makeEmptyList(), e);
	
	rewrite(lam);
};