package plaid.typechecker.context;

//add to the context differently if borrowed or not
val getContextType = fn (/*ChangeType*/ ct) => {
	ifElse (ct.borrow) 
		{ ct.input.copyAsBorrowed() }
		{ ct.input.copy() }
};