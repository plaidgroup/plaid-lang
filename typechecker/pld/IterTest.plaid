package coreExamples.iterTest;

import plaid.lang.*;

method main() {
	val list = java.util.LinkedList.new();
	list.add(1);
	list.add(2);
	list.add(3);
	val iter = list.iterator();
	while { iter.hasNext(); } {
		java.lang.System.out.println(iter.next());
	};
}