package testInputs.fibonacci;

method fib(n) {
	match (n <= 2) {
		case True {
			1;
		}
		
		default {
			fib(n - 1) + fib(n - 2);
		}
	};
}

method main() {
	var n = 10;
	var result = fib(n);
	java.lang.System.out.println("fib(" + n + ") = " + result);
}