package coreExamples.benchmarks.splaytree;

/* 
 * This benchmark test was ported from Version 6 of the benchmark suite 
 * for Google's V8 JavaScript engine.
 */
method main()  {
	var utils = new SplayBenchmarkUtils;
	utils.splayTreeSetup();
	utils.splayRun();
	utils.splayTreeTeardown();
}