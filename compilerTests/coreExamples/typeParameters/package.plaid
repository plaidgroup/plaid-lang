package coreExamples.typeParameters;

state Comparable<group protected owner, type S case of immutable Integer, type U>{
    method void bar<exclusive owner, type K>() {}
}

method void foo<type T case of immutable Comparable >() { }

method main() {
    foo<bar>().bar<foo>();
}