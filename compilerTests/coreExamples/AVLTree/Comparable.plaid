package coreExamples.AVLTree;

/* needs to be implemented by every element that is added to the tree as data object */
state Comparable {
    //method /* immutable ComparableResult */ compareTo (/* immutable >> immutable Comparable */ other) /* immutable */;
    method immutable ComparableResult compareTo <readonly owner>(shared<owner> Comparable >> shared<owner> Comparable other) [shared<owner> Comparable];
}
