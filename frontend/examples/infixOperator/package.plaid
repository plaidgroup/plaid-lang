package binaryOperator;

state Number {
    method immutable Number +(immutable Number other);
    method immutable Number -(immutable Number other);
    method immutable Number *(immutable Number other);
}

method main() {
    val immutable Number a = new Number;
    val immutable Number b = new Number;
    (a + b) * (b - a) 
}