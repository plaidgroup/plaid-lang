package plaid.compiler.passes;

import plaid.compiler.Pass;
import plaid.compiler.UserJob;
import plaid.compiler.CompilerJob;

state ReporterPass case of Pass {
    method immutable Boolean run(unique Job job) {
        match (job) {
            case UserJob {
                job.reporter.report();
                true
            }
            default { true }
        }
    }
}