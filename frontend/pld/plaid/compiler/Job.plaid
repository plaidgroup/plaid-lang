package plaid.compiler;

state Job {
    val unique Source     source;
    var unique Set        dependencies;
    var unique Set        dependents;
    var unique ASTNode    ast;
    var unique List       passes;
    var immutable Boolean status;
}