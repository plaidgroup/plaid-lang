package plaid.compiler.permissions;

/**
 *	BasePermissions contain all of the permissions that are 
 *  writable in the language.  Some are symmetric (they can
 *  be split into two copies of themselves, and others are singular
 *  meaning that only one reference with a singular permission can
 *	exist to a single object:
 *  Singluar: unique (, full)
 *	Symmetric: immutable, shared (,pure)
 */
state BasePermission case of SourcePermission { }