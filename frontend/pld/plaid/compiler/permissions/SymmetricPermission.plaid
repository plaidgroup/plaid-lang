package plaid.compiler.permissions;

/**
 *	Symmetric Permissions are base permissions that have
 *	the property that they can be freely duplicated so that
 *	SYMPERM splits into SYMPERM x SYMPERM
 *
 *  immutable, shared (,pure)
 */
state SymmetricPermission case of BasePermission { }