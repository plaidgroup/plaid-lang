package plaid.compiler.aeminium.tasks;

state Edge {
    val shared Task from;
    val shared Task to;
    
    method immutable String toString() {
        "(" + this.from.toString() +"," + this.to.toString() + ")"
    }
}