package plaid.compiler.types;

import plaid.compiler.PlaidType;

state NominalPlaidType case of StatePlaidType {
    val immutable PlaidType owner;
    val immutable String stateName;
    val immutable List<immutable MetaTypes> metaTypes;
    val immutable List<immutable PlaidType> memberTypes;
}