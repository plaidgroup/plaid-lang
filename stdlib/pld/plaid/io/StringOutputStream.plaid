package plaid.io;


state StringOutputStream<Character> case of OutputStream<Character>
{
	var stringOutputStreamBridgeVarible;
	
	method appendInteger(immutable Integer integerVariable)
	{
		stringOutputStreamBridgeVariable.append(integerVariable);
	}
	
	method appendBoolean(immutable Boolean booleanVariable)
	{
		stringOutputStreamBridgeVariable.append(booleanVariable);
	}
	
}