package plaid.ast.types;

/**
 *	top level interface for the class of types of objects (traditional types)
 *	this currently includes: Nominal Types, Strcutural Types, Lambda Types, and the Dynamic Type 
 */
state Type {
	
	method ==(/*Type*/ otherType);
	// TODO: What else do we need here?
	method subtypeOf(/*Type*/ other, /*StateTable*/ stateTable);
	
	method /*Type*/ join(/*Type*/ other, /*StateTable*/ stateTable);
	
	method toString();
}