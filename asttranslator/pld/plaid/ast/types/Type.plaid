package plaid.ast.types;

import plaid.ast.ASTNode;

state Type /*case of ASTNode*/ {
	//method accept(/*ASTVisitor*/ visitor) {
	//}
	
	method ==(/*Type*/ otherType);
	// TODO: What else do we need here?
	// method isSubtypeOf(Type otherType);
	
	method toString();
}