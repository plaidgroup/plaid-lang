package plaid.ast;

state UnannotatedBorrowBinding case of UnannotatedBinding {
	method accept(v) {
		v.visitBorrowBinding(this);
	}
	
	method copy() {
		new UnannotatedBorrowBinding {
			val /*Token*/ token = this.token;
			val /*ID*/ x = this.x.copy();
			val /*Expression*/ exp = this.exp.copy();
			val /*Expression*/ body = this.body.copy();
			val /*boolean*/ mutable = this.mutable;
		};
	}
}