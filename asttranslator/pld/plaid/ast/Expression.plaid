package plaid.ast;

import plaid.ast.types.*;

state Expression case of ASTNode {
	//each expression has its return type
	//it could be reuse by objectTypeFetch or other typechecker phase
	//it could be nil or something if there is type error within it
	var /*ObjectType*/ objectType = noType;
}