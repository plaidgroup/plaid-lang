package plaid.demo.plugin;

import java.util.ArrayList;

state Alive case of Liveness {

    /*
     * Toggles this cell in place.
     */
    method toggle() {
        this <- Dead;
    }
    
    /*
     * @returns a boolean representing whether this cell is alive
     */
    method isAlive() {
        true;
    }
}