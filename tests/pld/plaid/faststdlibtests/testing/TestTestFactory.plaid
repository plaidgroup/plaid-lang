package plaid.faststdlibtests.testing;

import plaid.testing.TestFactory;
import plaid.collections.makeLinkedList;

state TestTestFactory case of TestFactory {

	method createTests() {
		val tests = makeLinkedList();
		tests.add(this.mktest("emptyTest", fn(ctx) => unit));
		tests
	}

}