package plaid.fastexamples.statechange;

state Imago case of Butterfly {
    method fly () {
        printLine(this.name + " is flying.");
    }
    
    method layEgg(newName) {
        new Egg{val name = newName;};
    }
}