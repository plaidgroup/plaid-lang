package plaid.fastexamples.matchoption;

method void doMatch() {
    val unique MatchOption tester = new MatchOption { val immutable String s = "tester"; };
    
    printLine(tester.test(unit));
    printLine(tester.test(new MatchOption { val immutable String s = "foo"; } ));

}