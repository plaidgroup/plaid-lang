package plaid.typecheckertests.matchoptioncheck;

method void doMatch() {
    val unique MatchOption tester = new MatchOption { val immutable String s = "tester"; };
    
    tester.test(unit);
    tester.test(new MatchOption { val immutable String s = "foo"; } );

}