package plaid.benchmarks.simulation.WebServer;

state TestClient {
	method main() {
		val manager = new ServerManager;
	
		val httpServer = new Server { method responder(request, response) {  } val port = 8080; };
	}
}