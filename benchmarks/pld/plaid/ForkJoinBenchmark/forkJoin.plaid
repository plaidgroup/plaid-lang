package plaid.ForkJoinBenchmark;


method void forkJoinGlobal(immutable Integer level) {
    val immutable Integer currentLevel = level - 1;
    val immutable Boolean isZero = level == 0;
    
    match ( isZero ) {
        case True { 
            // nop 
        }
        case False {
            forkJoinGlobal(currentLevel);
            forkJoinGlobal(currentLevel);
        }
    }
}