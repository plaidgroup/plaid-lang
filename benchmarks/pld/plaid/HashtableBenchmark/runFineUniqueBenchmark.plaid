package plaid.HashtableBenchmark;

import plaid.examples.lib.hashtable.Hashtable;
import plaid.examples.lib.hashtable.fine.makeFineHashtable;

method void runFineUniqueBenchmark() {
    // create hashtable 
    val unique Hashtable ht = makeFineHashtable(6);
    
    // run benchmark
    runUniqueBenchmark(ht, 16, 8);
}