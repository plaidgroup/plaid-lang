package plaid.examples.webserver.plaid;

// unsafe
method immutable Boolean fileExists(immutable String path) {
    val file = java.io.File.new(getRoot() + path );
    
    file.exists() && file.isFile() && file.canRead()
}