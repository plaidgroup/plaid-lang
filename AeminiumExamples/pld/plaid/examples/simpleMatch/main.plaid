package plaid.examples.simpleMatch;

@sequential
method void main() {
    val unique SimpleMatch sm = new SimpleMatch;
    
    val immutable Integer trueValue = sm.simpleMatch(new True);
    java.lang.System.out.println("trueValue = " + trueValue.toString());
    val immutable Integer falseValue = sm.simpleMatch(new False);
    java.lang.System.out.println("falseValue = " + falseValue.toString());
}