package plaid.examples.transfer;


state Amount {}
state Account {}

method void withdraw(unique Account account, immutable Amount amount) {}
method void deposit(unique Account account, immutable Amount amount) {}

method void transfer(unique Account from,
                     unique Account to,
                     immutable Amount amount) {
    withdraw(from, amount);
    deposit(to, amount);                     
}
