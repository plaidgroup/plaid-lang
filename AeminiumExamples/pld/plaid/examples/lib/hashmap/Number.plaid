package plaid.examples.lib.hashmap;

state Number case of Hashable {
    var immutable Integer value = 0;

    @sequential
    override method immutable Integer hash() [ immutable Number this ]  {
        this.value
    }
    
    @sequential
    method immutable String toString()  [ immutable Number this ]  {
        val immutable Integer strValue = this.value;
        strValue.toString();
    }
}