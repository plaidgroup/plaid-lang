package plaid.examples.lib.hashtable.global;

import plaid.examples.lib.hashtable.Hashable;

state Bucket {
    method void add(immutable Hashable obj, immutable Integer index) [ shared Bucket this ];
    method immutable Boolean contains(immutable Hashable obj, immutable Integer index) [ shared Bucket this ];
}