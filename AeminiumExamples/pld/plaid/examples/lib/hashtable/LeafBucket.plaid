package plaid.examples.lib.hashtable;

state LeafBucket case of Bucket {
    // TODO: add list 
    method void add<group shared owner>(immutable Hashable obj, immutable Integer index) [ shared<owner> LeafBucket this] {
    
    }
}